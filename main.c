/* Fake source code
 *
 * Not very good at all. It doesn't do anything but dumb things. This is a very long line. Maybe the review tool can tell us how long it is and if it is a problem or not.
 *
 * This paragraph is wrapped properly. It's full of nonsense and junk still,
 * however. Is the horse correct? Is the battery stapled? No, but the horse is
 * stabled and the battery level is correct. */

int banana = 0x00BA7A7A;
extern int ding_dong;

int main(int argv, char *argv[])
{
    if( banana != ding_dong ) {
        return -1;
    }

    /* Trailing whitespace again! */

    return 0;
}
